.references

  &__nav
    ul
      display block
      text-align center
      li
        display inline-block
        width 30px
        margin 0.25em 1em
        padding 0
        text-align center
        
        a
          cursor pointer
  
  &__header
    h1
      color: #322f30;
      font-size: 38px;
      margin: 1em 0;
      padding-top: 0;
    
    &__logo
      display block
      text-decoration none
      width 100%
      height 100px
      background: url("/images/afesg-logo.png") no-repeat center center;
      background-size: contain;
      
  &__inner
    display block
    padding 35px 75px
    max-width 1000px
    margin 0 auto
    /* These are technically the same, but use both */
    overflow-wrap: break-word;
    word-wrap: break-word;

    -ms-word-break: break-all;
    /* This is the dangerous one in WebKit, as it breaks things wherever */
    word-break: break-all;
    /* Instead use this non-standard one: */
    word-break: break-word;

    /* Adds a hyphen where the word breaks, if supported (No Blink) */
    -ms-hyphens: auto;
    -moz-hyphens: auto;
    -webkit-hyphens: auto;
    hyphens: auto;
    
    @media (max-width $mobile-max)
      padding 35px 20px
      
    
    h2
      font-size 26px
      margin 1em 0 0.25em
      padding-bottom 0.3em
      border-bottom 1px solid $gray-light
    
    p, ul, li
      font-size 0.95em
      line-height 1.5
      margin 1.2em 0
    
    a
      text-decoration none
      color darken($central-africa, 20%)
      &:hover
        text-decoration underline
    
    &__back
      width 22px
      height 22px
      position fixed
      top 20px
      left 20px
      background transparent url(/images/icon-close.svg) no-repeat center center
      background-size contain
      cursor pointer
      opacity 0.65
      transition opacity 0.25s
      &:hover
        opacity 1
        transition opacity 0.25s